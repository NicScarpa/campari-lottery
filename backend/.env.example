# Configurazione Server
PORT=3001

# Configurazione Database
# Development: SQLite (file locale)
DATABASE_URL="file:./dev.db"
# Production: PostgreSQL URL da Railway
# DATABASE_URL="postgresql://username:password@hostname:5432/database_name"

# Sicurezza
# IMPORTANTE: Genera un secret casuale forte per production!
# Usa: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this"

# URL del Frontend (serve per i link nei QR code e per i permessi CORS)
FRONTEND_URL="http://localhost:3000"
